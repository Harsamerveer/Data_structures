"""
Card
Query
"""


# Add Method
def add(x, y):
    return x + y


def divide(x, y):
    if y == 0:
        raise ValueError("Cannot not divide by Zero!")
    return x / y


def multiply(z, a):
    if z or a == 0:
        raise ValueError("Cannot not multiply by Zero!")
    return z / a

    # Binary Search and Linear Search
# Create a function to Locate Card


# We'll represent our test cases as dictionaries to make it easier to test them once we write implement our function.
